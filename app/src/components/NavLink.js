import React from 'react';
import { Link } from 'react-router';

const NavLink = (props) => {
    return <Link {...this.props} className="nav--btn" activeClassName="nav--btn__active" />;
}

export default NavLink;